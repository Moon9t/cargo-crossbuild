[package]
name = "crossbuild-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Orchestrator that drives the full cross-build pipeline"
keywords.workspace = true
categories.workspace = true

[dependencies]
crossbuild-core = { path = "../core", version = "1.0.0" }
crossbuild-planner = { path = "../planner", version = "1.0.0" }
crossbuild-runner = { path = "../runner", version = "1.0.0" }
toml = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }