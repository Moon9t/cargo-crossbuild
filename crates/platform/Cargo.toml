[package]
name = "crossbuild-platform"
version = "0.1.0"
edition = "2021"

[dependencies]
crossbuild-core = { path = "../core" }
anyhow = { workspace = true }
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
semver = { workspace = true }